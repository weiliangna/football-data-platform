# 用户中心性能与可靠性报告

日期：2026-08-28
范围：用户列表、用户详情、S/A/B 评级、首单标签和前端请求行为。
限制：本轮未连接生产 MySQL、未执行 migration、未请求第三方平台，因此不伪造生产 SQL 耗时、P95 或并发指标。

## 1. 原始问题

- 旧等级接口在用户循环中逐人查询最近战绩，生产只读基线为 28.407 秒。
- 用户详情一次读取 100 张订单并执行完整比赛与赛果关联，生产只读基线为 3.734 秒。
- 用户中心缺少服务端筛选、请求取消、输入防抖与局部错误状态。
- 页面中存在多个人工刷新入口，快速重复点击会制造并发请求。
- 首单金额标签若直接把当前库最早订单当作完整历史首单，会误标老用户。

## 2. 根因分析

- 等级计算的主要问题是 N+1 查询和重复全历史扫描。
- 用户详情返回范围超过当前弹窗需求，放大赛果关联成本和响应体积。
- 搜索把数字用户 ID 放进前后模糊匹配，无法使用精确键查询。
- 前端缺少 in-flight 请求取消，筛选和搜索快速变化时旧响应可能覆盖新响应。
- 当前 schema 没有可证明第三方历史已完整同步的字段，不能生成“已确认首单”标签。

## 3. TOP 慢接口（已有生产只读基线）

| 接口 | 修改前总耗时 | 本轮状态 |
|---|---:|---|
| `/api/hub/users?limit=5` | 28.407s | N+1 路径由两条批量 SQL 和 30 秒缓存替代 |
| `/api/portal/dashboard` | 12.032s | 前序版本已隔离线程池并加入 single-flight 缓存 |
| `/api/portal/analysis` | 8.172s | 本轮未改业务聚合 |
| `/api/portal/heatmap` | 7.859s | 本轮仅移除人工刷新入口 |
| `/api/portal/user/1/302798` | 3.734s | 详情限制最近 20 单，并保留真实赛果关联 |

这些是修改前的历史取证，不是本轮修改后的生产数字。代码尚未部署，不能把离线测试时间当作生产接口耗时。

## 4. TOP 慢 SQL 与风险

1. 完整订单腿关联 `match_results` 的 identity v2 与 legacy fallback。
2. Dashboard 的待开奖订单聚合（前序版本已改为轻量腿查询）。
3. 用户评级原逐用户最近 5 单查询（本轮已删除运行路径）。
4. 用户评级的累计奖金仍以 `SUM(platform_bonus)` 精确计算，但由独立批量查询完成，不再逐用户查询。
5. 用户评级原全历史 `GROUP_CONCAT`（本轮改读 `user_statistics.recent_results`）。
6. 用户详情的最近订单赛果关联（仍保留，范围从 100 限制为 20）。
7. 用户列表的玩法 `EXISTS` 筛选。
8. 首单筛选中的最早有效订单相关子查询。
9. 当前页用户的首单聚合与玩法聚合。
10. `COALESCE(publish_time, created_time)` 日期条件可能影响单列索引使用。

没有生产 `EXPLAIN ANALYZE`，以上按源码结构排序，不能声称为生产真实耗时排名。

## 5. 数据库优化

- 本轮未新增索引和 migration。
- 数字用户 ID 搜索改为 `us.user_id = ?`；昵称继续使用模糊查询。
- 最近 7 日条件同时排除未来时间。
- 用户列表先服务端分页，再只为当前页批量读取派生指标。
- 首单身份按 `platform_id + user_id`，时间相同时以订单 ID 稳定排序。

需要生产只读取证后再判断玩法、首单和日期组合索引，避免在未知数据规模下锁表。

## 6. 后端优化

- S/A/B 算法集中在 `common/user_grading.py`，权重、分位、ROI 收缩、贝叶斯命中、最近 5 单与硬门槛保持单一来源。
- 评级读取收敛为一条 7 日批量聚合和一条累计奖金/等级/人工覆盖批量查询。
- 评级结果使用进程内 30 秒 TTL 和互斥重建，避免同一时刻重复计算。
- 用户列表新增服务端筛选、真实近期指标、月回报、今日发单和今日跟单人数。
- 用户详情限制最新 20 张方案，按发单时间倒序并返回比赛腿、SP、赛果、预计回报与倍数。
- 当前数据库没有跟单金额字段，API 明确返回 `null`，不使用 `自购金额 × 跟单人数` 伪造。

## 7. 前端优化

- 搜索输入 450ms 防抖。
- 列表与详情请求均支持 AbortController，路由销毁时取消未完成请求。
- 请求设置 15 秒上限；后台更新保留已有表格，不切回整页骨架。
- 点击用户行才加载详情，鼠标悬浮不发请求，消除列表闪动来源。
- 失败仅影响当前模块，不清空仍可用数据。
- 所有人工刷新按钮和相关文案已从活动 Vue 页面移除。

## 8. 缓存优化

- S/A/B 评级：30 秒进程内缓存，互斥重建。
- Dashboard：沿用前序版本 15 秒新鲜缓存、120 秒 stale-while-revalidate。
- 未引入 Redis，避免在没有部署与容量证据时增加运行依赖。

## 9. 修改文件

- `common/user_grading.py`
- `common/user_labels.py`
- `api/hub.py`
- `api/portal.py`
- `frontend/src/views/UserCenter.vue`
- 用户详情及各页面人工刷新入口相关 Vue 文件
- `tests/test_user_directory_v6.py`

## 10. 新增索引

无。原因是未连接生产数据库，缺少 `SHOW INDEX`、表规模和 `EXPLAIN ANALYZE` 证据。

## 11. 修改前后指标

| 指标 | 修改前 | 修改后可验证结果 |
|---|---|---|
| 等级数据查询模式 | 逐用户最近战绩 N+1 + 全历史扫描 | 2 条批量 SQL，结果缓存 30 秒 |
| 用户详情订单数 | 100 | 20 |
| 数字 ID 搜索 | 前后模糊匹配 | 精确匹配 |
| 快速筛选请求 | 旧请求可继续运行 | 新请求发出前取消旧请求 |
| 搜索请求 | 每次输入都可能触发 | 450ms 防抖 |
| 人工刷新入口 | 多页面存在 | 活动 Vue 页面为 0 |

生产 After、P50、P95、P99、50/100 并发错误率必须在部署后通过只读压测取得，本轮没有伪造。

## 12. 剩余风险

- PyMySQL 仍是按请求建连，没有连接池；需先采集生产连接峰值。
- 首单历史完整性没有 schema 证据，因此当前只返回 `suspected` 和疑似标签。
- 自动评级和首单派生结果使用短缓存而非数据库持久化；人工等级继续由现有表持久化。
- 跟单总额缺少真实源字段，只能显示 `--`。
- 昵称前后模糊搜索仍可能扫描；需结合数据规模决定前缀索引或专用检索。
- 完整赛果关联仍需生产 `EXPLAIN ANALYZE` 后再优化 legacy fallback。

## 13. 后续建议

1. 部署后记录用户列表、用户详情、Dashboard 的 20 次串行和 1/10/50/100 并发基线。
2. 只读采集表行数、索引、慢查询和连接峰值。
3. 确认各平台历史同步完整性来源，再设计可回滚 migration 持久化首单事实与自动评级快照。
4. 获取真实跟单金额字段后再接入“跟单总额”。
5. 根据 `EXPLAIN ANALYZE` 决定索引，不直接在生产猜测建索引。
